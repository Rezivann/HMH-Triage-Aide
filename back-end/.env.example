NODE_ENV=development
PORT=4000

DATABASE_URL=mongodb://localhost:27017/llmtriage

# app.js - frontend dev server origin (Vite default)
CORS_ORIGIN=http://localhost:5173

# kioskAuth.js
KIOSK_API_KEY_SECRET=

# nurseAuth.js / DuoAuthService.js - Cisco Duo SSO (Generic OIDC Relying
# Party application). Client ID/Secret from the application's Client tab;
# the four URLs from its Metadata tab (issuer/authorization/token/jwks).
DUO_CLIENT_ID=
DUO_CLIENT_SECRET=
DUO_ISSUER=
DUO_AUTHORIZATION_URL=
DUO_TOKEN_URL=
DUO_JWKS_URL=
# Must exactly match the Redirect URI configured on the Duo application.
DUO_REDIRECT_URI=http://localhost:4000/dashboard/duo-callback
DUO_STATE_SECRET=dev-insecure-duo-state-secret
NURSE_SESSION_SECRET=dev-insecure-nurse-secret
# duo-callback redirects the browser here after minting a nurse session.
FRONTEND_URL=http://localhost:5173

# trackAuth.js
TRACK_TOKEN_SECRET=dev-insecure-track-secret

# photoAuth.js - short-lived, session-scoped token embedded in the kiosk's QR
# code so a patient's phone can submit a photo without ever holding the
# device's own long-lived KIOSK_API_KEY_SECRET
PHOTO_TOKEN_SECRET=dev-insecure-photo-secret

# LlmService - Anthropic Messages API
LLM_API_KEY=
LLM_MODEL=claude-haiku-4-5
LLM_API_VERSION=2023-06-01

# CvServiceClient -> ml-service
CV_SERVICE_URL=http://localhost:8000

# TranscriptionService - OpenAI Whisper, fallback speech-to-text for
# browsers with no SpeechRecognition API (e.g. Webex Desk)
OPENAI_API_KEY=

# ContactCenterService - Webex Contact Center
WEBEX_CC_CLIENT_ID=
WEBEX_CC_CLIENT_SECRET=
WEBEX_CC_ORG_ID=

# voiceAuth.js - Cisco Webex AI Agent Studio's webhook (voice channel).
# Placeholder shared-secret scheme until a real Agent Studio tenant exists
# and its actual webhook contract is known - see voiceAuth.js.
WEBEX_VOICE_WEBHOOK_SECRET=

# queueSort.js - decay rates and caps are per-category now, nurse-editable
# from the dashboard, so they live in fakeAcuityPolicyStore.js, not here
